# Frontend Environment Variables
# Supabase URL and Anon Key are primarily used for client-side auth if needed,
# but most data fetching will go through our backend API proxy.
# These might not be strictly necessary if Supabase client is not used directly in frontend.

# VITE_SUPABASE_URL=YOUR_SUPABASE_URL
# VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Backend API URL (handled by Vite proxy, but good for reference)
# VITE_API_BASE_URL=http://localhost:3001/api
